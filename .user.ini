; PHP-FPM per-directory settings (Hostinger / cPanel shared hosting)
; This file is read by PHP-FPM; .htaccess php_value lines are ignored in FPM mode.
upload_max_filesize = 10M
post_max_size       = 12M
max_execution_time  = 60
memory_limit        = 256M
